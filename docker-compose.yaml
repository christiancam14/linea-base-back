version: '3'

services:
  db:
    image: mysql:latest
    restart: always
    ports:
      - "3316:3316"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
    container_name: baseline
    volumes:
      - ./mysql:/var/lib/mysql